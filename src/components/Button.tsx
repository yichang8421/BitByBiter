import styled from "styled-components";

const Button = styled.button`
            color: red;
            border: none;
            background: green;
            &:hover{
              background: #000;
            }
            `;

export default Button;
